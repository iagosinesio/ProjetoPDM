<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.iago.zooquiz">

    <application
            android:allowBackup="true"
            android:icon="@drawable/logo_final"
            android:roundIcon="@drawable/logo_final"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">

        <activity android:name=".MainActivity" android:theme="@style/LoginPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        
        <activity android:name=".SobreActivity" android:theme="@style/LoginPage">
            
        </activity>

        <activity android:name=".CadastroDePerguntaActivity" android:theme="@style/LoginPage">

        </activity>

    </application>

</manifest>